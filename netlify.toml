# Netlify config. The official @netlify/plugin-nextjs runtime is auto-detected
# and handles SSR, API routes, and middleware — no extra wiring needed.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
